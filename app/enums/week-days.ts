export enum WeekDays {
    // mesmo que o tipo enum atribua valores as variaveis na ordem apresentada,
    // eh bom declarar tambem seu valor para que nao haja o risco da ordem da lista ser
    // alterada, e consequentemente alterar o funcionamento de seu uso em outras
    // partes do codigo

    SUNDAY = 0,
    MONDAY = 1,
    TUESDAY = 2,
    WEDNESDAY = 3,
    THURSDAY = 4,
    FRIDAY = 5,
    SATURDAY = 6
}